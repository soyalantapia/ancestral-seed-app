# react-simple-maps@3 declara peer react <=18, pero el proyecto corre React 19.
# El árbol resuelve bien igual; legacy-peer-deps evita que `npm install` aborte
# (el lockfile ya está generado con esta estrategia; `npm ci` no se ve afectado).
legacy-peer-deps=true
